---
name: attention
description: "Use when you have been given prior consent to get the users attention after some long running task"
---

The `cc-notify` utility sends push notifications directly to the user, see `cc-notify --help`. Use only when given prior consent with sentiment of 'get my attention' or 'notify me'.

please look at @pi/agent.njk and then run the Pi --debug-prompt flag with various outputs so i can validate my templating

config for the njk file is at:
~/dev/projects/agents/pi-extensions/dynamic-agents-md/README.md

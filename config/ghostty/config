# Run `ghostty +show-config --default --docs` to view a list of
# ghostty +validate-config

keybind = super+r=reload_config

font-family = Victor Mono
font-size = 14
font-thicken = true
font-thicken-strength = 50

font-feature = salt=2
font-feature = cv01=1
font-feature = cv02=1
font-feature = cv06=1
font-feature = cv14=1
font-feature = +zero
font-feature = +onum
font-feature = +ss04
font-feature = cv18=1
font-feature = cv30=1
font-feature = +ss09
font-feature = +ss07

# `theme` writes the selected family/mode here, then asks Ghostty to reload.
# Follow the system appearance with Tokyo Night until the first selection.
theme = light:TokyoNight Day,dark:TokyoNight Moon
config-file = ?active-theme.conf

macos-titlebar-style = hidden
window-padding-x = 4
window-padding-y = 2


shell-integration-features = ssh-terminfo,ssh-env

background-opacity = 0.95
custom-shader = ~/.config/ghostty/shaders/snow_cursor_impulse.glsl
# custom-shader = ~/.config/ghostty/shaders/embers.glsl
# Draw the short Neovide-style cursor smear on top of the snow.
custom-shader = ~/.config/ghostty/shaders/cursor_smear.glsl
# custom-shader = /Users/adamhall/dev/vendor/ghostty-shaders/cursor_lightning.glsl
# Animate only while focused ("always" also animates unfocused terminals).
custom-shader-animation = true

# Remap Cmd+Shift+[ / ] to Ctrl+Alt+z / Ctrl+Alt+x for tmux prev/next window
keybind = super+shift+left_bracket=text:\x1b\x1a
keybind = super+shift+right_bracket=text:\x1b\x18
# quick terminal replace window switch (tmux so don't have multiple)
keybind = global:cmd+backquote=toggle_quick_terminal

command = ~/.config/ghostty/startup.sh

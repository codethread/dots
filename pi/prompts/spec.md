---
description: read specs then go
---

- study specs/README.md
- use subagents when a single tool use yourself (would not suffice)
  - `explorer` to find code
  - `shell` to perform bash based tool usage
  - `worker` to make targeted code modifications

$@

#!/usr/bin/env bash
# :module: fzf wrapper to kill processes

pid=$(ps -ef | sed 1d | eval "fzf ${FZF_DEFAULT_OPTS} -m --header='[kill:process]'" | awk '{print $2}')

if [ -n "$pid" ]; then
  echo "$pid" | xargs kill -"${1:-9}"
  kp "$@"
fi

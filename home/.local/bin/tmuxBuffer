#!/usr/bin/env bash
# :module: tmux clipboard and buffer management utility

file=$(mktemp)
tmux capture-pane -pS -32768 >"$file"
line_count=$(wc -l <"$file" | tr -d ' ')
tmux new-window -n:history "nvim -u NONE -R -c \"lua require('codethread.kitty+page')($line_count, $line_count, 1)\" '$file'"
